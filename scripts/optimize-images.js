// Image optimization script removed per user request.
// The original script used sharp to generate AVIF/WebP/resized variants.
// It has been disabled to keep the repo using the original images under `public/images`.
// If you want to re-enable image optimization later, restore or replace this file.
console.log('optimize-images.js: disabled');
